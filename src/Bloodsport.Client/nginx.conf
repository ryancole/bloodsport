events { worker_connections 1024; }
http {
    include /etc/nginx/mime.types;
    server {
        listen 80;
        root /usr/share/nginx/html;
        index index.html;
        # Blazor WASM — all routes fall back to index.html
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
