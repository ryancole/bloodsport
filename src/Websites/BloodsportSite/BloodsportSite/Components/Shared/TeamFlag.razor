@using Bloodsport.Entity.Database
@inject BloodsportSite.Services.BlobSasService BlobSasService

@if (flagUrl is not null)
{
    <img src="@flagUrl" width="28" height="18" class="flex-shrink-0 rounded-[2px] object-cover ring-1 ring-black/20" alt="" />
}
else
{
    <span class="inline-flex items-center justify-center flex-shrink-0 rounded-[2px] ring-1 ring-black/20 bg-gray-100 text-gray-400"
          style="width:28px;height:18px;">
        <svg width="11" height="11" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
            <path d="M8 1 2 3v5c0 4 3 6 6 7 3-1 6-3 6-7V3z" />
        </svg>
    </span>
}

@code {
    [Parameter, EditorRequired] public string? LogoUrl { get; set; }

    private string? flagUrl => BlobSasService.GetSasUrl(LogoUrl?.Replace("/original.", "/flag."));
}
