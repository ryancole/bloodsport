Dashboard KPI tile — a gradient-filled Archivo Black numeral over a mono uppercase label, topped by the signature keyline.

```jsx
<StatCard value={12} label="Active seasons" />
<StatCard value="5–1" label="Best record" />
```

Use in a `repeat(3, 1fr)` grid with a 14px gap for the standard dashboard stat row.
