Small square avatar for teams and players. Renders the logo image when present, else a gradient tile with up-to-two-letter initials.

```jsx
<TeamFlag name="Void Reavers" logoUrl={team.logoUrl} size={28} />
<TeamFlag name="Ryan Cole" />
```

Use inline before team/player names in tables, rosters, and activity feeds.
